type keyInput = "up" | "down" | "left" | "right" ; // type is a way to provide the type->data which can be passed to the function  

//now i can also use "ENUS" for


function doSomething(keyPressed: keyInput ){
    //do something
    if(keyPressed =="up"){

    }
    // some more operation
}

doSomething("up");